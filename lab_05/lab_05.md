# Лабораторная работа №5.

## Оптимизация запросов с помощью индексов и анализа плана выполнения.

### 1. Титульный лист.

- Студент: Войнова Екатерина Андреевна
- Группа: ЦИБ-241

### 2. Цель работы.

Научиться анализировать производительность SQL-запросов, интерпретировать план выполнения (Query Plan) и оптимизировать работу базы данных с помощью различных типов индексов (B-tree, Hash).

### 3. Задачи.

- Изучить принципы работы планировщика запросов (Query Planner) в PostgreSQL.
- Освоить команды `EXPLAIN` и `EXPLAIN ANALYZE` для диагностики производительности.
- Сравнить методы сканирования таблиц: последовательное сканирование (Sequential Scan) и сканирование по индексу (Index Scan).
- Научиться создавать и применять индексы для ускорения выборок.

---

## 4. Ход выполнения работы.

### Задание 1. Анализ на сервере. Найти товары (products) типа product_type = 'scooter' (Индекс B-Tree).

Скриншот выполнения запроса.

<img width="744" height="194" alt="image" src="https://github.com/user-attachments/assets/8d61a1fb-3869-4db3-9a51-0e225b5df4a8" />

Вывод. Используется Seq Scan (полный перебор), время 0,047 мс.

### Задание 2. Индекс локально. Найти товары (products) типа product_type = 'scooter'. (Индекс B-Tree).

Скриншот выполнения запроса.

<img width="1020" height="304" alt="image" src="https://github.com/user-attachments/assets/0ed07ba2-bec9-40b9-bde7-957d3878c9cf" />

Вывод. Время сократилось в 3,6 раз, используется индекс.

### Задание 3. Сложная оптимизация локально - JOIN/Range. Оптимизировать поиск клиентов с postal_code = '33111'.

Скриншот выполнения запроса.

<img width="649" height="364" alt="image" src="https://github.com/user-attachments/assets/858ed7cf-ce1a-48fc-92dd-72fd7ec130ee" />

Вывод. Комбинация индекса и JOIN-подзапроса заставляет базу данных использовать быстрый алгоритм Hash Join. Это превращает медленный перебор всей таблицы в мгновенное сопоставление в памяти, что максимально ускоряет поиск по конкретному postal_code.

Файл с SQL-командами. https://github.com/VoinovaEA/prog/blob/main/Script.sql5.sql

Выводы: В ходе выполнения задач я освоила инструменты EXPLAIN и EXPLAIN ANALYZE для диагностики PostgreSQL и на практике сравнила методы сканирования таблиц. На примере поиска товаров типа scooter я убедилась, что замена полного перебора (Seq Scan) на сканирование по индексу (Index Scan) сокращает время выполнения запроса, что доказывает эффективность применения B-Tree индексов для оптимизации выборок.
